{
  "name": "@easycompute/workspace",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": "24.18.1",
    "npm": "11.16.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run build && npm run test --workspaces --if-present",
    "prepublishOnly": "node -e \"console.error('Workspace root is private and must not be published.'); process.exit(1)\"",
    "verify:packaged-install": "node scripts/verify-packaged-install.mjs",
    "verify:os-keyring": "npm run build --workspace=@easycompute/cli && node scripts/verify-os-keyring.mjs",
    "verify:security": "npm run build && node --test packages/easycompute/test/secret-store.test.mjs packages/easycompute/test/plugin-credentials.test.mjs packages/easycompute/test/state-store.test.mjs packages/easycompute/test/local-daemon.test.mjs packages/easycompute/test/ssh-access-adapter.test.mjs packages/easycompute/test/connection-gateway.test.mjs && node --test plugins/vastai/test/provider.test.mjs plugins/intelion/test/provider.test.mjs plugins/intelion/test/access.test.mjs",
    "release:artifacts": "node scripts/build-release-artifacts.mjs",
    "release:check": "npm run typecheck && npm test && npm pack --workspaces --dry-run && npm run verify:packaged-install && npm run release:artifacts",
    "easycompute": "npm run start --workspace=@easycompute/cli --"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "@types/semver": "7.7.1",
    "typescript": "6.0.2"
  }
}
